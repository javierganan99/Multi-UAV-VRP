var map; // Map
var markers = []; // Markers
var infoWindow; // Info of the markers
var paths = []; // Routes
var vehicleImages = {}; // A map to store img elements by vehicle id
var vehicleSockets = {}; // A map to store socket connections by vehicle id